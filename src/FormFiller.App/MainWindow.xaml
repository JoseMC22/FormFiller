<Window x:Class="FormFiller.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:FormFiller.App.Views"
        mc:Ignorable="d"
        Title="FormFiller" Height="700" Width="1200" Background="{StaticResource BackgroundBrush}">
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="TrialStatusText" Foreground="{StaticResource TextMutedBrush}" />
            </StatusBarItem>
        </StatusBar>
        <TabControl>
            <TabItem Header="Inspección">
                <views:InspectorView />
            </TabItem>
            <TabItem Header="Mapeo de campos">
                <views:MappingView />
            </TabItem>
            <TabItem Header="Ejecución">
                <views:RunnerView />
            </TabItem>
            <TabItem Header="Recetas">
                <views:RecipeView />
            </TabItem>
            <TabItem Header="Grabador">
                <views:RecorderView />
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
