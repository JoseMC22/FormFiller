<UserControl x:Class="FormFiller.App.Views.MappingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Header="Saved Templates" Grid.Column="0" Margin="0,0,4,0">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,6">
                        <Button Content="Refresh templates" Command="{Binding LoadTemplatesCommand}"
                                Padding="10,4" />
                        <Button Content="Load mappings" Command="{Binding LoadMappingsCommand}"
                                Margin="8,0,0,0" Padding="10,4" />
                    </StackPanel>
                    <ListView ItemsSource="{Binding Templates}"
                              SelectedItem="{Binding SelectedTemplate}"
                              Height="110">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" Text="{Binding Name}" />
                                    <TextBlock FontSize="11" Foreground="Gray"
                                               Text="{Binding WindowTitle}" TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </DockPanel>
            </GroupBox>

            <GroupBox Header="Excel Data" Grid.Column="1" Margin="4,0,0,0">
                <StackPanel>
                    <DockPanel Margin="0,0,0,6">
                        <Button DockPanel.Dock="Right" Content="Open Excel..."
                                Command="{Binding OpenExcelCommand}" Padding="10,4" Margin="6,0,0,0" />
                        <TextBox Text="{Binding ExcelFilePath}" IsReadOnly="True"
                                 VerticalContentAlignment="Center" />
                    </DockPanel>
                    <DockPanel Margin="0,0,0,6">
                        <TextBlock Text="Sheet:" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <ComboBox ItemsSource="{Binding Sheets}"
                                  SelectedItem="{Binding SelectedSheet}" />
                    </DockPanel>
                    <DockPanel>
                        <TextBlock Text="Columns:" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <ListBox ItemsSource="{Binding Columns}" Height="70" />
                    </DockPanel>
                </StackPanel>
            </GroupBox>
        </Grid>

        <GroupBox Grid.Row="1" Header="Field to Column Mapping" Margin="0,8,0,0">
            <DataGrid ItemsSource="{Binding MappingRows}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Field name" Width="*"
                                        Binding="{Binding FieldName}"
                                        IsReadOnly="True" />
                    <DataGridTextColumn Header="Field type" Width="0.7*"
                                        Binding="{Binding FieldType}"
                                        IsReadOnly="True" />
                    <DataGridComboBoxColumn Header="Excel column" Width="1.2*"
                                            SelectedItemBinding="{Binding SelectedColumn, UpdateSourceTrigger=PropertyChanged}"
                                            ItemsSource="{Binding DataContext.Columns, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <DockPanel Grid.Row="2" Margin="0,8,0,0">
            <Button DockPanel.Dock="Left" Content="Save mappings"
                    Command="{Binding SaveMappingsCommand}" Padding="12,6" />
            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Foreground="DimGray"
                       Text="{Binding StatusMessage}" TextWrapping="Wrap" />
        </DockPanel>
    </Grid>
</UserControl>
